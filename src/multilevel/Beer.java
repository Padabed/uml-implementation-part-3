package multilevel;

public class Beer extends Drink {
    public Beer(double volume, Double sugarAmount, Double strength, DrinkType drinkType) {
        super(volume, sugarAmount, strength, drinkType);
    }
}
