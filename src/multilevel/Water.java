package multilevel;

public class Water extends Drink {
    public Water(double volume, Double sugarAmount, Double strength, DrinkType drinkType) {
        super(volume, sugarAmount, strength, drinkType);
    }
}
